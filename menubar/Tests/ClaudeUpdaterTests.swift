import Testing

// NOTE: @testable import ClaudeBotMenu is added in Task 3/4 when unit-testable
// symbols (ClaudeUpdater) are extracted into a library target. The executable
// target cannot be @testable-imported until then.

final class ClaudeUpdaterTests {
    @Test func testPlaceholder() {
        #expect(true, "SPM scaffolding works — real tests added in later tasks")
    }
}
